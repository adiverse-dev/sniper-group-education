import { IMAGE_PATHS } from "./imagePaths";

// Backward-compatible branding export.
export const BRANDING_ASSETS = {
  navbarLogo: IMAGE_PATHS.branding.logo,
  footerLogo: IMAGE_PATHS.branding.logo,
};
